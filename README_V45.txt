FLEVO Website v4.5

Updates:
1. Quotation form fixed: public requests are inserted into quotation_requests without requiring public SELECT permission.
2. Quotation form works from the home page and project details page.
3. Full Arabic/English switching on public pages with persistent language selection.
4. High-resolution approved FLEVO logo included:
   - assets/flevo-logo-horizontal.png for headers
   - assets/flevo-logo-transparent.png for footer/brand use
5. Project, gallery, filters, project details and quotation messages change with the selected language.

Run using Live Server, then hard refresh with Ctrl+Shift+R.
Test quote requests in Supabase: Table Editor > quotation_requests.
