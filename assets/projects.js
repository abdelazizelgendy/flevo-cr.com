let allProjects=[],activeCat='all';const grid=document.getElementById('grid'),empty=document.getElementById('empty'),search=document.getElementById('search'),resultsCount=document.getElementById('resultsCount'),projectTotal=document.getElementById('projectTotal');const fallback='assets/flevo-logo-transparent.png';
FlevoStore.getProjects().then(d=>{allProjects=(d||[]).filter(p=>p.published!==false).sort((a,b)=>(a.order??999)-(b.order??999));projectTotal.textContent=allProjects.length;render()}).catch(err=>{console.error(err);empty.hidden=false;empty.innerHTML='<strong>تعذر تحميل المشروعات</strong>تأكد من الاتصال بقاعدة البيانات ثم أعد تحميل الصفحة.'});
function safe(v){return String(v??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]))}
function render(){const en=window.FlevoI18n?.lang==='en',t=(ar,enText)=>en?(enText||ar):ar,q=(search.value||'').trim().toLowerCase();const list=allProjects.filter(p=>{const matchesCat=activeCat==='all'||p.category===activeCat||activeCat==='manufacturing'&&p.category==='fabrication';const hay=`${p.titleAr||''} ${p.titleEn||''} ${p.client||''} ${p.location||''} ${p.locationEn||''} ${p.year||''}`.toLowerCase();return matchesCat&&hay.includes(q)});resultsCount.textContent=en?`Showing ${list.length} of ${allProjects.length}`:`عرض ${list.length} من ${allProjects.length}`;grid.innerHTML=list.map(p=>{const title=en?(p.titleEn||p.titleAr):p.titleAr,loc=en?(p.locationEn||p.location):p.location,cat=en?(p.categoryEn||p.categoryAr):p.categoryAr;return `<article class="pcard"><a href="project.html?id=${encodeURIComponent(p.id)}" aria-label="${safe(title)}"><div class="cover-wrap">${p.featured?`<span class="featured">${t('مشروع مميز','Featured Project')}</span>`:''}<img src="${safe(p.cover||fallback)}" alt="${safe(title)}" loading="lazy" onerror="this.onerror=null;this.src='${fallback}'"></div><div class="pbody"><span class="tag">${safe(cat||'FLEVO')}</span><h3>${safe(title||t('مشروع بدون عنوان','Untitled Project'))}</h3><div class="meta"><span>${safe(p.client||'FLEVO')}</span><span>•</span><span>${safe(loc||'')}</span>${p.year?`<span>•</span><span>${safe(p.year)}</span>`:''}</div><span class="view">${t('عرض تفاصيل المشروع','View Project')} <span>←</span></span></div></a></article>`}).join('');empty.hidden=list.length>0}
search.addEventListener('input',render);document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');activeCat=b.dataset.cat;render()}));document.addEventListener('flevo:language',render);
