let data=[],active='all',visible=[],idx=0,query='';const g=document.getElementById('gallery'),m=document.getElementById('modal'),mi=m.querySelector('img'),cap=document.getElementById('modalCaption'),count=document.getElementById('galleryCount');function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
FlevoStore.getProjects().then(d=>{data=d.filter(p=>p.published!==false).sort((a,b)=>(a.order||999)-(b.order||999));document.getElementById('projectCount').textContent=data.length;render()}).catch(e=>{console.error(e);g.innerHTML='<div class="empty"><strong>تعذر تحميل المعرض</strong><span>راجع الاتصال بقاعدة البيانات.</span></div>'});
function render(){const en=window.FlevoI18n?.lang==='en',q=query.trim().toLowerCase(),ps=(active==='all'?data:data.filter(p=>p.category===active||active==='manufacturing'&&p.category==='fabrication')).filter(p=>!q||`${p.titleAr} ${p.titleEn} ${p.client}`.toLowerCase().includes(q));visible=ps.flatMap(p=>{const arr=p.images&&p.images.length?p.images:[p.cover];return arr.filter(Boolean).map((src,n)=>({src,title:en?(p.titleEn||p.titleAr):p.titleAr,client:p.client,id:p.id,index:n+1,total:arr.length}))});document.getElementById('imageCount').textContent=visible.length;count.textContent=en?`Showing ${visible.length} images`:`عرض ${visible.length} صورة`;if(!visible.length){g.innerHTML=`<div class="empty"><strong>${en?'No matching images':'لا توجد صور مطابقة'}</strong><span>${en?'Try another search or category.':'جرّب تغيير البحث أو التصنيف.'}</span></div>`;return}g.innerHTML=visible.map((x,i)=>`<figure class="gallery-card" data-i="${i}"><img src="${x.src}" alt="${esc(x.title)}" loading="lazy"><figcaption><strong>${esc(x.title)}</strong><span>${esc(x.client||'FLEVO')}</span></figcaption></figure>`).join('');g.querySelectorAll('.gallery-card').forEach(x=>x.onclick=()=>show(+x.dataset.i))}
function show(i){idx=(i+visible.length)%visible.length;mi.src=visible[idx].src;cap.textContent=`${visible[idx].title} — ${visible[idx].index} / ${visible[idx].total}`;m.classList.add('open')}document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.cat;render()});document.getElementById('gallerySearch').addEventListener('input',e=>{query=e.target.value;render()});document.addEventListener('flevo:language',render);m.querySelector('.close').onclick=()=>m.classList.remove('open');m.querySelector('.prev').onclick=e=>{e.stopPropagation();show(idx-1)};m.querySelector('.next').onclick=e=>{e.stopPropagation();show(idx+1)};m.onclick=e=>{if(e.target===m)m.classList.remove('open')};document.addEventListener('keydown',e=>{if(!m.classList.contains('open'))return;if(e.key==='Escape')m.classList.remove('open');if(e.key==='ArrowRight')show(idx-1);if(e.key==='ArrowLeft')show(idx+1)});
